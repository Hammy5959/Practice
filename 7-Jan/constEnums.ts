//Const Enums
const enum Color {
  red,
  green,
  blue,
}
let colorName:Color=Color.red
console.log(colorName)
//Start With 0 but const enums does not accept value method. const enums only accept key method.
